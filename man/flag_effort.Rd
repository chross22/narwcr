% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effort.R
\name{flag_effort}
\alias{flag_effort}
\title{Flag on-effort records}
\usage{
flag_effort(
  dat,
  max_beaufort = 3,
  max_alt_m = 366,
  min_visibility_nmi = 2,
  legtype_on_effort = 2L,
  na_action = c("fail", "pass")
)
}
\arguments{
\item{dat}{A data frame of NARWC survey data, ideally from \code{\link[=read_narwc]{read_narwc()}}.}

\item{max_beaufort}{Highest acceptable Beaufort sea state. Default \code{3}.}

\item{max_alt_m}{Highest acceptable aircraft altitude in metres. Default
\code{366}, which is 1,200 feet.}

\item{min_visibility_nmi}{Minimum acceptable visibility in nautical miles.
Default \code{2}, the CETAP standard.}

\item{legtype_on_effort}{Integer vector of \code{LEGTYPE} values that count as
effort. Default \code{2}.}

\item{na_action}{What to do when a criterion's value is missing: \code{"fail"}
(default, treat the record as off effort) or \code{"pass"} (ignore the missing
criterion).}
}
\value{
\code{dat} with an integer \code{OnOff.Effort} column added: \code{1} on effort,
\code{0} off effort.
}
\description{
Determines, for each record, whether the platform was on effort: on a
designated census track, in survey conditions good enough for the sightings
to be usable in a density estimate.
}
\section{Criteria}{

A record is on effort when all of the following hold:
\itemize{
\item \code{LEGTYPE} is in \code{legtype_on_effort} — by default \code{2}, a line-transect
survey line (handbook 8.A.21). Transits, cross-legs, and circling do not
contribute effort to a density estimate.
\item \code{BEAUFORT} is at most \code{max_beaufort}.
\item \code{ALT} is below \code{max_alt_m}, in metres (handbook 8.A.1).
\item Visibility clears \code{min_visibility_nmi}, via \code{\link[=visibility_ok]{visibility_ok()}}.
}

A criterion whose column is absent from the data is skipped, with a message.
A criterion whose value is \code{NA} fails, unless \code{na_action = "pass"}.

The defaults are the CETAP standard. Kenney and Winn (1986, p. 347) state the
criteria applied to that programme's data as "observer(s) formally on watch,
clear visibility of at least 2 miles, and sea states of Beaufort 3 or lower";
surveys were flown at 750 ft (229 m) (p. 346). The handbook (8.A.38) confirms
2 nautical miles as the standard for acceptable survey conditions defined
during CETAP. Every threshold here is an argument, because a different
programme may reasonably choose differently.

Records are \emph{not} removed. Off-effort records are retained because the
distance between consecutive on-effort positions still needs them for
correct effort accounting, and because sightings made while circling are
attached back to the segment they came from.
}

\examples{
path <- system.file("extdata", "narwc-example.csv", package = "narwcr")
dat <- flag_effort(read_narwc(path))
table(dat$OnOff.Effort)

# A stricter sea-state cutoff
table(flag_effort(read_narwc(path), max_beaufort = 2)$OnOff.Effort)

}
\references{
Kenney, R.D. and Winn, H.E. (1986) Cetacean high-use habitats of the
northeast United States continental shelf. \emph{Fishery Bulletin} 84(2):345-357.

CETAP (1982) \emph{A Characterization of Marine Mammals and Turtles in the Mid- and
North-Atlantic Areas of the U.S. Outer Continental Shelf, Final Report.}
Cetacean and Turtle Assessment Program, University of Rhode Island. Bureau of
Land Management, Washington, DC.

Kenney, R.D. (2023) \emph{The North Atlantic Right Whale Consortium Database: A
Guide for Users and Contributors, Version 8}. NARWC Reference Document
2023-01.
}
\seealso{
\code{\link[=visibility_ok]{visibility_ok()}}, \code{\link[=on_effort_census_rows]{on_effort_census_rows()}}
}
