% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{narwc_checks}
\alias{narwc_checks}
\title{The handbook-general check set}
\usage{
narwc_checks()
}
\value{
A named list of functions.
}
\description{
Returns the checks that \code{\link[=validate_narwc]{validate_narwc()}} runs by default: those that
follow from the NARWC handbook itself, and so are true of any NARWC extract
regardless of what it will be used for.
}
\section{Adding your own}{

A check is a function of one argument, the data frame, returning findings
built with \code{\link[=narwc_finding]{narwc_finding()}} — \code{NULL}, one finding, or a list of them.
Because the set is an ordinary named list, a package adds to it by
concatenating:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{validate_narwc(dat, checks = c(narwc_checks(), my_checks()))
}\if{html}{\out{</div>}}

and drops one it does not want by name:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{keep <- setdiff(names(narwc_checks()), "time_format")
validate_narwc(dat, checks = narwc_checks()[keep])
}\if{html}{\out{</div>}}

Checks that depend on what the data will be \emph{used for} belong in the package
that uses it, not here. \code{distsamp} keeps the ones about declination angles
and exact sighting positions for that reason: they are only problems if you
are computing a right-angle distance.
}

\examples{
names(narwc_checks())

}
