% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-narwc.R
\name{narwc_column_mapping}
\alias{narwc_column_mapping}
\title{What the column names were changed to}
\usage{
narwc_column_mapping(dat)
}
\arguments{
\item{dat}{A data frame from \code{\link[=read_narwc]{read_narwc()}} or
\code{\link[=standardize_narwc_columns]{standardize_narwc_columns()}}.}
}
\value{
A tibble with \code{original}, \code{standardized}, \code{match}, and \code{factor}.
\code{match} is either \code{"alias"} for an exact entry in the alias table or
\code{"inferred"} for one found after normalising case and separators. \code{factor}
is the multiplier applied to reach the canonical unit — \code{0.3048} for an
altitude that arrived in feet — and \code{NA} where the values were taken as
they were. Zero rows when nothing was renamed.
}
\description{
The record of how \code{\link[=read_narwc]{read_narwc()}} or \code{\link[=standardize_narwc_columns]{standardize_narwc_columns()}} renamed a
data frame's columns, so a rename can be checked rather than trusted.
}
\section{Why keep it}{

Matching ignores case and separators, which is what makes a real export
readable without hand-editing — and also what makes it possible for a column
to be renamed onto something you did not intend. The mapping travels with the
data as an attribute so the question "where did this column come from" has an
answer after the fact, not only in a message that has scrolled away.
}

\examples{
raw <- data.frame(Event = 1, Lat_DD = 43, Long_DD = -69, Sea_State = 3)
dat <- standardize_narwc_columns(raw, quiet = TRUE)
narwc_column_mapping(dat)

}
\seealso{
\code{\link[=read_narwc]{read_narwc()}}, \code{\link[=standardize_narwc_columns]{standardize_narwc_columns()}}
}
