% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/declination.R
\name{angles_from_declination}
\alias{angles_from_declination}
\title{Build ANGLEL and ANGLER from one declination column and a side}
\usage{
angles_from_declination(
  dat,
  angle,
  side,
  left = "L",
  right = "R",
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{dat}{A data frame, ideally from \code{\link[=read_narwc]{read_narwc()}}.}

\item{angle}{Name of the column holding the declination angle, in degrees.}

\item{side}{Name of the column saying which side the sighting was on.}

\item{left, right}{The values of \code{side} meaning left and right. Compared
after trimming whitespace and ignoring case.}

\item{overwrite}{Replace values already in \code{ANGLEL}/\code{ANGLER}? Default
\code{FALSE}, which fills only the blanks — a recorded angle is not displaced by
a derived one.}

\item{quiet}{Suppress the report of what was set and what was skipped.
Default \code{FALSE}.}
}
\value{
\code{dat} with \code{ANGLEL} and \code{ANGLER} added or filled.
}
\description{
The handbook records a declination angle in \code{ANGLEL} or \code{ANGLER} according
to which side of the aircraft the sighting was on (8.A.2). Some survey
programmes instead keep a single angle column and a separate left/right
flag. This splits the one into the two.
}
\section{Why this is not automatic}{

\code{narwcr} will not infer meaning from a column name, and this mapping cannot
be inferred safely. A column called \code{Decl_Angle} might hold a declination
below the horizon, an inclination above it, or a bearing; \code{Left_or_Right}
might use \code{L}/\code{R}, \code{1}/\code{2}, or \code{port}/\code{starboard}. Naming the columns and
the codes is the caller asserting what they mean, which is a different thing
from the package guessing.
}

\section{What it checks}{

A declination angle is measured down from the horizontal, so \code{perp_distance()}
computes \code{ALT / tan(angle)}: 90 degrees is directly below the aircraft and
gives a perpendicular distance of zero, and an angle at or below 0 is
undefined. Values outside \verb{(0, 90]} are reported and left alone rather than
converted into a distance that cannot be right.

Records whose side is missing or unrecognised are reported and skipped: an
angle with no side cannot be placed, and guessing a side would put sightings
on the wrong half of the track line.
}

\examples{
dat <- data.frame(
  SPECCODE = c("RIWH", "RIWH", "FIWH"),
  Decl_Angle = c(40, 20, 90),
  Left_or_Right = c("L", "R", "R")
)
angles_from_declination(dat, "Decl_Angle", "Left_or_Right")

}
\references{
Kenney, R.D. (2023) \emph{The North Atlantic Right Whale Consortium Database: A
Guide for Users and Contributors, Version 8}, section 8.A.2. NARWC Reference
Document 2023-01.
}
\seealso{
\code{distsamp::perp_distance()}, which consumes these columns.
}
