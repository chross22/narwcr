% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effort.R
\name{track_speed}
\alias{track_speed}
\title{Speed implied by consecutive position fixes}
\usage{
track_speed(dat, by = NULL, max_gap = 300)
}
\arguments{
\item{dat}{A data frame with \code{LATITUDE}, \code{LONGITUDE} and \code{TIME}, in survey
order.}

\item{by}{Columns identifying a stretch to compute within, so speed is never
taken across a break. \code{NULL} (default) uses \code{LEGNO3} when present, else
\code{DATE} and \code{FILEID}.}

\item{max_gap}{Ignore intervals longer than this many seconds. Default
\code{300}. A long gap is a break in the record rather than slow travel, and
including it drags the speed down.}
}
\value{
A numeric vector of knots, one per row: the speed from that record
to the next. \code{NA} at the end of each stretch and wherever the interval is
unusable.
}
\description{
The distance from each record to the next, divided by the time between them.
Computed from what the receiver logged rather than from any speed column, so
it is available on any file carrying positions and a clock.
}
\section{Why this exists}{

Nothing in a NARWC file reliably says what the platform was. \code{PLATFORM} is
optional, has no code book, and is empty on real extracts; \code{LEGTYPE} codes
5 and 6 mark shipboard records but a mixed file may use the aerial codes
throughout. Speed cannot be mistaken for something else: a survey aircraft
flies at 90-120 knots and a vessel surveys at about 10, and no arrangement
of the other columns makes one look like the other.
}

\examples{
path <- system.file("extdata", "narwc-example.csv", package = "narwcr")
dat <- make_leg_id(read_narwc(path, quiet = TRUE), quiet = TRUE)
summary(track_speed(dat))

}
\seealso{
\code{\link[=classify_platform]{classify_platform()}}, which turns this into a platform label.
}
