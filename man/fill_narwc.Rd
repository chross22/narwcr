% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.R
\name{fill_narwc}
\alias{fill_narwc}
\title{Carry survey state forward into blank rows}
\usage{
fill_narwc(
  dat,
  columns = narwc_fill_columns(),
  by = NULL,
  direction = c("downup", "down", "up"),
  quiet = FALSE
)
}
\arguments{
\item{dat}{A data frame of NARWC survey data, in survey order.}

\item{columns}{Columns to fill. Defaults to \code{\link[=narwc_fill_columns]{narwc_fill_columns()}}. Anything
in \code{\link[=narwc_never_fill]{narwc_never_fill()}} is an error rather than a silent omission.}

\item{by}{Grouping columns. \code{NULL} (default) uses \code{FILEID} and \code{DATE} where
present.}

\item{direction}{\code{"downup"} (default), \code{"down"}, or \code{"up"}, as
\code{\link[tidyr:fill]{tidyr::fill()}}.}

\item{quiet}{Suppress the report of what was filled. Default \code{FALSE}.}
}
\value{
\code{dat} with the requested columns filled.
}
\description{
NARWC data often records a value once and leaves it blank until it changes —
\code{LEGTYPE} is entered as \code{2} at the start of a census line and the rows beneath
it are empty until the leg type changes. This fills those blanks, \strong{within a
survey day and file}, so that downstream code sees the state each record was
actually flown under.
}
\section{Grouping is the whole point}{

An ungrouped fill runs the length of the file. The sea state from the last
record of one survey day carries into the first records of the next; a leg
number carries across a \code{FILEID} boundary into a different survey entirely.
Neither is recoverable afterwards, because the filled value is
indistinguishable from a recorded one.

The scripts this package was rewritten from filled these columns with no
grouping at all (\code{DataExploration.R:52}, \verb{:71}). \code{by} defaults to \code{FILEID} and
\code{DATE}, and if neither is present this function warns rather than quietly
filling across everything.
}

\section{Direction, and what "up" actually does}{

\code{tidyr::fill()} semantics. \code{"down"} is the direction the recording convention
justifies: a blank means "as above".

\code{"downup"} fills down first and then fills up, so the only values it fills
backwards are those \emph{before the first recorded value in a group}. That is a
smaller claim than it sounds, but it is still a guess: the state before
anything was logged is genuinely unknown, and back-filling asserts it matched
whatever was recorded first. Where a day's records begin on transit before the
first \code{LEGTYPE} is entered, back-filling a \code{2} would mark that transit as
census effort.

Because those are the inferred values rather than the recovered ones, the
report counts them separately. A large backward count is worth looking at.
}

\section{A caution on \code{LEGSTAGE}}{

Filling \code{LEGSTAGE} down is the least safe of the defaults. If a file records
\code{1} (begin line) and leaves the continuation rows blank, filling down marks
every record of that line as "begin line" rather than \code{2} (continue) — and
since on-effort eligibility is \code{LEGSTAGE == 2}, the whole line would drop out
of every distance calculation. Whether that happens depends on the recording
convention of the file in hand. Check the \code{LEGSTAGE} counts in the report
against what you expect before trusting a filled column.
}

\examples{
dat <- data.frame(
  FILEID = "A", DATE = as.Date("2024-04-01"), EVENTNO = 1:5,
  LEGTYPE = c(2, NA, NA, 1, NA), BEAUFORT = c(3, NA, NA, NA, NA)
)
fill_narwc(dat)

# Refuses to replicate a sighting
try(fill_narwc(dat, columns = "NUMBER"))

}
\references{
Kenney, R.D. (2023) \emph{The North Atlantic Right Whale Consortium Database: A
Guide for Users and Contributors, Version 8}. NARWC Reference Document
2023-01.
}
\seealso{
\code{\link[=narwc_fill_columns]{narwc_fill_columns()}}, \code{\link[=read_narwc]{read_narwc()}}, \code{\link[=validate_narwc]{validate_narwc()}}
}
