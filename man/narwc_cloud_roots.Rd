% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cloud.R
\name{narwc_cloud_roots}
\alias{narwc_cloud_roots}
\title{Where OneDrive and Google Drive sync to}
\usage{
narwc_cloud_roots()
}
\value{
A tibble with \code{service}, \code{path}, and \code{exists}.
}
\description{
The conventional sync-root locations for this platform, and whether each is
present. Most cloud-stored survey data is reachable as an ordinary file path,
and this is how you find it.
}
\section{Nothing is searched for}{

Only the documented locations are checked — this does not scan your home
directory. If your sync root is somewhere else, pass its path directly; every
function here takes an ordinary path.

On macOS both clients now sync under \verb{~/Library/CloudStorage}, with a folder
per account (\code{OneDrive-UniversityofMaine}, \code{GoogleDrive-you@example.com}).
The older \verb{~/OneDrive} and \verb{~/Google Drive} locations are still checked
because existing installations keep them.
}

\examples{
narwc_cloud_roots()

# Only the ones actually present
subset(narwc_cloud_roots(), exists)

}
\seealso{
\code{\link[=narwc_fetch]{narwc_fetch()}}, \code{\link[=read_narwc]{read_narwc()}}
}
