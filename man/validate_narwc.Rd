% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_narwc}
\alias{validate_narwc}
\title{Check survey data against the NARWC handbook}
\usage{
validate_narwc(dat, checks = narwc_checks())
}
\arguments{
\item{dat}{A data frame of NARWC survey data, ideally from \code{\link[=read_narwc]{read_narwc()}}.}

\item{checks}{A named list of check functions. Defaults to \code{\link[=narwc_checks]{narwc_checks()}}.}
}
\value{
A tibble with one row per problem found, and columns:
\describe{
\item{\code{check}}{Name of the check, as listed above.}
\item{\code{severity}}{\code{"error"}, \code{"warning"}, or \code{"note"}.}
\item{\code{column}}{The column involved, or \code{NA}.}
\item{\code{n}}{Number of records affected.}
\item{\code{rows}}{List column of affected row indices (capped at 100).}
\item{\code{message}}{Human-readable description.}
}
A zero-row tibble means every check passed.
}
\description{
Runs a set of structural and code-book checks against a standardised NARWC
data frame and reports every problem found. Validation never stops on error
and never modifies the data: the result is a report you read, so that you can
decide which problems matter for your analysis.
}
\section{Checks performed}{

By default, the handbook-general set from \code{\link[=narwc_checks]{narwc_checks()}}:
\describe{
\item{\code{missing_required}}{A column in \code{\link[=narwc_schema]{narwc_schema()}}\verb{$required} is absent.
Severity \code{error}.}
\item{\code{missing_values}}{\code{NA} in a required column.}
\item{\code{unknown_code}}{A value of \code{LEGTYPE}, \code{LEGSTAGE}, \code{IDREL}, \code{TAXCODE},
or \code{STRATUM} that is not in the handbook's code book.}
\item{\code{legstage_off_census}}{\code{LEGSTAGE} recorded on a record that is not a
census line. Handbook 8.A.20: for dedicated aerial surveys \code{LEGSTAGE} is
recorded only during census tracks (\code{LEGTYPE == 2}), except for code 7.}
\item{\code{sighting_at_boundary}}{A sighting recorded at a \code{LEGSTAGE} of 1, 3,
4, or 5. Handbook 8.A.20 and 4.2: sightings should not occur at
begin-line, break-off, resume, or end-line events.}
\item{\code{legstage_sequence}}{\code{LEGSTAGE} does not follow a logical order
within a line occupation. Handbook 8.A.20: a line begins (1), continues
(2), may break off to circle (3) and resume (4), and ends (5). A line
must begin with 1, nothing may follow an end-line, and a resume cannot
appear without a break-off before it.}
\item{\code{legstage_break_off_unresumed}}{A line ends at a break-off (3) with
no resume and no end-line: the aircraft left the census line and the
record never brings it back.}
\item{\code{legstage_line_not_closed}}{A line has no end-line (5). A note
rather than a warning, because a line abandoned for weather or re-flown
later legitimately has none.}
\item{\code{eventno_not_increasing}}{\code{EVENTNO} does not increase through a
\code{FILEID}. Repeated values are allowed — the handbook (4.2) assigns one
event several sightings — but decreases indicate mis-sorted records.}
\item{\code{bad_time_format}}{\code{TIME} is not a 6-digit \code{hhmmss} in 24-hour form
(handbook 8.A.37). Four-digit \code{hhmm} times are reported separately as a
warning since they are still found in older data.}
\item{\code{coordinates_out_of_range}}{Latitude outside [-90, 90] or longitude
outside [-180, 180].}
\item{\code{positive_west_longitude}}{Every longitude is positive. Handbook
8.A.22 requires west longitudes to be negative; all-positive longitudes
in a western North Atlantic dataset mean the sign convention was lost.}
\item{\code{sighting_without_number}}{\code{SPECCODE} present but \code{NUMBER} missing.
Handbook 8.A.24 requires \code{NUMBER} for all sightings.}
\item{\code{exact_position_out_of_range}}{\code{S_LAT} or \code{S_LONG} outside the range
a coordinate can take. Handbook 8.A.33 and 8.A.34 give the exact sighting
position in decimal degrees.}
\item{\code{columns_outside_handbook}}{Columns present that are not NARWC
handbook variables. Survey programmes add their own; they are carried
through uninterpreted, and one that encodes position, effort, or distance
must be mapped explicitly. See \code{\link[=narwc_profiles]{narwc_profiles()}}.}
}

Checks specific to a particular analysis live with that analysis; see
\code{\link[=narwc_checks]{narwc_checks()}} for how to add them.
}

\examples{
path <- system.file("extdata", "narwc-example.csv", package = "narwcr")
issues <- validate_narwc(read_narwc(path))
issues[, c("check", "severity", "n")]

# Run only the code-book checks
validate_narwc(read_narwc(path), checks = narwc_checks()["code_books"])

}
\references{
Kenney, R.D. (2023) \emph{The North Atlantic Right Whale Consortium Database: A
Guide for Users and Contributors, Version 8}. NARWC Reference Document
2023-01. University of Rhode Island, Graduate School of Oceanography. Every
check above cites the section it derives from.

Kenney, R.D. (2002) \emph{Quality-control Issues for Data Submissions to the North
Atlantic Right Whale Consortium Database.} NARWC Reference Document 2002-02.
}
\seealso{
\code{\link[=narwc_checks]{narwc_checks()}} for the default set, \code{\link[=narwc_finding]{narwc_finding()}} for writing
your own.
}
