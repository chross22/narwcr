% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legstage.R
\name{fill_legstage}
\alias{fill_legstage}
\title{Reconstruct the line state on records that record no LEGSTAGE}
\usage{
fill_legstage(dat, by = NULL, quiet = FALSE)
}
\arguments{
\item{dat}{A data frame with \code{LEGSTAGE}, in survey order — from
\code{\link[=make_leg_id]{make_leg_id()}}, which sorts.}

\item{by}{Columns identifying one line occupation, so a state never carries
across a break. \code{NULL} (default) uses \code{LEGNO3} when present, else \code{DATE}
and \code{FILEID}.}

\item{quiet}{Suppress the report of how many were filled. Default \code{FALSE}.}
}
\value{
\code{dat} with \code{LEGSTAGE} filled where the state is known, and a logical
\code{LEGSTAGE_FILLED} marking every record this function wrote to.
}
\description{
\code{LEGSTAGE} marks what is happening to a survey line — it begins (1),
continues (2), breaks off to circle (3), resumes (4), ends (5). Programmes
record it when it \emph{changes}, so a record taken mid-line often carries none
at all. This fills in the state those records were in.
}
\section{Why this is not \code{\link[=fill_narwc]{fill_narwc()}}}{

Carrying \code{LEGSTAGE} forward as a value is wrong. \code{1} means "begin line", an
event; copying it onto the next thousand records claims the line began a
thousand times, and since \code{1 -> 1} is not a legal transition it manufactures
the sequence errors it looks like it should fix.

What carries forward is the \emph{state}, not the code. Handbook 8.A.20:

\tabular{ll}{
after 1 begin \tab the line is continuing, so 2 \cr
after 2 continue \tab still continuing, 2 \cr
after 4 resume \tab continuing again, 2 \cr
after 3 break off \tab off the line, circling — left \code{NA} \cr
after 5 end \tab the line is over — left \code{NA} \cr
before any event \tab unknown — left \code{NA}
}

So the only code ever written is \code{2}, and only where the line is genuinely
continuing. A record before the first event, during a circle, or after the
line closed keeps its \code{NA} and stays ineligible — which is correct, because
a detection made while not searching the line breaks the distance-sampling
assumptions rather than merely the bookkeeping.
}

\section{What it is for}{

\code{on_effort_census_rows()} requires \code{LEGSTAGE == 2}, and handbook 8.A.31
restricts a right-angle distance measurement to records in that state. On a
real archive 1,928 of 2,280 on-effort census sightings carried no
\code{LEGSTAGE}, so 85\% of the detections that could have informed a detection
function were excluded for a code nobody wrote down.
}

\examples{
dat <- data.frame(
  FILEID = "F", EVENTNO = 1:6, LEGNO3 = "1_1", LEGTYPE = 2,
  LEGSTAGE = c(1, NA, NA, 5, NA, NA)
)
fill_legstage(dat, quiet = TRUE)$LEGSTAGE

}
\references{
Kenney, R.D. (2023) \emph{The North Atlantic Right Whale Consortium Database: A
Guide for Users and Contributors, Version 8}, sections 8.A.20 and 8.A.31.
NARWC Reference Document 2023-01.
}
\seealso{
\code{\link[=on_effort_census_rows]{on_effort_census_rows()}}, which is what this makes answerable;
\code{\link[=fill_narwc]{fill_narwc()}} for columns whose \emph{value} carries forward.
}
