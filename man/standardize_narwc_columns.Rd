% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-narwc.R
\name{standardize_narwc_columns}
\alias{standardize_narwc_columns}
\title{Standardise column names onto the NARWC handbook's}
\usage{
standardize_narwc_columns(dat, quiet = FALSE, prefer_source = TRUE)
}
\arguments{
\item{dat}{A data frame.}

\item{quiet}{Suppress the report of inferred matches. Default \code{FALSE}.}

\item{prefer_source}{Let a \verb{Trk*} GPS track column take precedence over a
canonical column of the same name. Default \code{TRUE}. See \code{\link[=read_narwc]{read_narwc()}}.}
}
\value{
\code{dat} with columns renamed where a match was found.
}
\description{
Renames a data frame's columns to their canonical NARWC names, matching
case-insensitively and ignoring separators. This is the step \code{\link[=read_narwc]{read_narwc()}}
does first, exported on its own so that other packages working with NARWC
survey data can use the same vocabulary rather than maintain a second one.
}
\section{Why this is shared}{

Every pipeline that reads real survey exports hits the same wall: the files
say \code{Event}, \code{event_no}, or \verb{Event No.} and the code wants \code{EVENTNO}. Solving
it once per package means the vocabularies drift, and a column recognised in
one place is silently dropped in another. The alias table here is the merge
of two such attempts — \code{distsamp}'s and \code{msomgom}'s — and this package exists
so that it is the only one that grows.
}

\section{What it will not do}{

Guess by edit distance. \code{EVENTN0} with a zero stays \code{EVENTN0}, because a
column that is nearly a name is not that name. It also never renames onto a
canonical column that already exists, so a correctly named column always
wins, and it warns when two columns could both be one canonical name.

Policy stays with the caller. This renames columns and nothing else: it does
not fill defaults, drop records, or coerce types. \code{read_narwc()} does those,
and packages with different needs keep their own on top. The standing example
is \code{ALT}: \code{msomgom} defaults a missing altitude to a nominal survey height,
which is reasonable for occupancy but would be wrong in \code{distsamp}, where
\code{ALT} feeds a right-angle distance and a fabricated altitude gives a
fabricated distance. Neither default belongs here.
}

\examples{
raw <- data.frame(Event = 1, Lat = 43, Long = -69, Sea_State = 3,
                  check.names = FALSE)
names(standardize_narwc_columns(raw, quiet = TRUE))

}
\seealso{
\code{\link[=read_narwc]{read_narwc()}}, \code{\link[=narwc_schema]{narwc_schema()}}
}
