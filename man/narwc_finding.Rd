% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{narwc_finding}
\alias{narwc_finding}
\title{Record one validation finding}
\usage{
narwc_finding(
  check,
  severity,
  column = NA_character_,
  rows = integer(0),
  message,
  n = length(rows)
)
}
\arguments{
\item{check}{Name of the check, used as the \code{check} column.}

\item{severity}{\code{"error"}, \code{"warning"}, or \code{"note"}.}

\item{column}{The column involved, or \code{NA}.}

\item{rows}{Integer vector of affected row indices. Stored capped at 100.}

\item{message}{Human-readable description of the problem.}

\item{n}{Number of records affected. Defaults to \code{length(rows)}; give it
explicitly for a finding that is about the table rather than particular
rows, such as a column being absent altogether.}
}
\value{
A one-row tibble.
}
\description{
Builds a single row of the report that \code{\link[=validate_narwc]{validate_narwc()}} returns. Packages
that add their own checks use this so that their findings have the same
shape as the handbook-general ones and can be bound together with them.
}
\examples{
narwc_finding(
  "my_check", "warning", "ALT", rows = c(3L, 7L),
  message = "ALT looks implausible."
)

}
